pyspark==3.5.1
pyyaml==6.0.2
pandas==2.2.2
pytest==8.2.0


# Standard library covers everything else (sqlite3, csv, unittest).
# Tests run with `python -m unittest discover -s tests`, and are also
# pytest-discoverable if you have pytest installed locally.
